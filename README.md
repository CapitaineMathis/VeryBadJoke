# VeryBadJoke

A simple discord bot who make very French bad joke...

The commande `/joke` call the jokeapi for a joke.

---

For using it you can just use Docker :
```
docker compose up -d --build
```